/** @public Magic SSE control event name used by the library. */
export const MagicEventType = "__MAGIC_EVENT__";

/** @public Current protocol version for magic/control messages. */
export const MagicProtocolVersion = 1;
